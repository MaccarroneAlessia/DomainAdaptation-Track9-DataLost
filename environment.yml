name: dl-project
channels:
  - pytorch
  - nvidia
  - defaults
dependencies:
  - python=3.10
  - pip
  - pytorch
  - torchvision
  - torchaudio
  - pytorch-cuda=11.8 
  # Add other conda packages here

  - pip:
      - numpy
      - pandas
      - matplotlib
      - scikit-learn
      - pyyaml
      - tensorboard
      - tqdm
      # Add other pip-only packages here
      - wandb
      - pytorch_lightning
      - pytorchvideo

      # pip install --user nel container
